import styled from 'styled-components';

const CurrentAmount = styled.div`
	align-self: center;
	text-align: center;
	font-weight: 700;
	width: 2ch;
	overflow-x: auto;
`;

export default CurrentAmount;
